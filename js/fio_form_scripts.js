function validateClientForm() {
    var isValid = true;
    document.getElementById("result").innerHTML = "lolka";

    if (isValid) {
        submitClientForm();
    }
}

function submitClientForm() {
    //Здесь будет происходить отправка формы
}